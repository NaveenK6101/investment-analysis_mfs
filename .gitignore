# Raw scratch downloads - large, easily re-fetched, not real deliverables.
# They stay on disk for local reuse; just not worth carrying in git history.
data/portfolios/
data/all_schemes.json

# Python bytecode cache, nbconvert run logs - regenerated on every run
__pycache__/
*.pyc
*.log

# Unrelated personal files (nested its own .git, was never a real submodule -
# broke GitHub Pages' checkout step trying to resolve a URL-less gitlink).
# Contains personal documents - never re-add this, especially now the repo
# is public.
old-vahdam-files/
data/sif_snapshots/
